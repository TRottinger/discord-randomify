from setuptools import setup

setup(
   name='discord_bot_randomizer',
   version='0.0.1',
   description='A discord bot that handles',
   author='Tim Rottinger',
   author_email='trrottinger@gmail.com',
   packages=['discord_bot_randomizer']  #same as name
)